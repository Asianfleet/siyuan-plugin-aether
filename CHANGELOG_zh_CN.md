<p align="center">
    <img src="asset/aether-thin.png" alt="catenaconf logo" width=100%/>
</p>

[1.x 版本 changelog](https://github.com/Asianfleet/siyuan-plugin-aether/blob/main/CHANGELOG.md)

## v2.4.0

- 新增**链接上下文类型**：
  - 粘贴到输入框的链接内容会被自动解析为上下文附件（注意：暂不支持思源块超链接，如果粘贴内容是含有链接的文本，则要保证链接被空格包裹，否则会被视为普通文本）
  - 超链接右键菜单中添加了“添加到 Aether 输入”选项，可直接将选中的超链接添加到输入框
- 在图片菜单、面包屑更多菜单中添加了“添加到 Aether 输入”选项
- 优化了内部数据加载逻辑

## v2.3.1

- 优化内部配置加载逻辑
- 修复与 toolkit-plugin-siyuan 插件的兼容性问题（与该插件同时使用时，会导致 Aether 无法读取插件配置）

## v2.3.0

- 确保 API 密钥**使用 ISO-8859-1 编码**并进行 URL 编码处理：在调用在线模型接口时，对 API 密钥进行了编码兼容性处理
  - 应对错误1：[Aether] Failed to start stream:Failed to execute 'append' on 'Headers': String contains non ISO-8859-1 code point. v3.3.6 
  - 应对错误2：获取模型列表失败：Failed to construct 'Headers': String contains non ISO-8859-1 code point
- **添加块内容去重功能**以解决重复内容问题：当选中的块包含标题时，“添加到 Aether 输入”选项会导致内容重复
- 修复插件侧栏图标**拖动后行为异常**问题：当插件处于对话状态，此时拖动侧栏图标会导致插件操作失效
- 插件样式优化：更新了多个若干组件的 CSS 类名前缀，统一命名空间，增强样式隔离性

## v2.2.1

- 改进模型上下文使用率组件：当前模型未设置上下文窗口时，则不显示组件
- 改进插件配置加载逻辑
- 优化用户消息工具栏样式

## v2.2.0

- **对话记录的标签将自动生成**
- 修复插件图标只有在右上角时才能正常打开独立窗口的问题
- 修复 changelog 界面的 1.x 版本链接
- 插件样式优化

## v2.1.1

- 新增**推理内容折叠功能**：开启后，推理内容在思考完成后自动折叠，点击展开后显示完整内容
- 优化输入框样式

## v2.1.0

- 新增**消息历史导出功能**
- 新增**用户消息折叠功能**：开启后，用户一次性输入的消息过长会自动折叠，折叠后仅显示部分内容，点击展开后显示完整内容
- 当把思源笔记的 Tab 拖动到输入框时，若该笔记处于聚焦状态，则**只提取聚焦部分作为上下文**

## v2.0.1

- **支持 top_p 参数**
- 修复了与 kmind 插件的兼容性问题

## v2.0.0

**重构插件代码**

* 界面样式优化
* 新增 rules、command 特性
* 输入框
  - 新增显示当前对话上下文消耗情况
  - 新增剪贴板，可临时存储文字
* 对话界面
  - 舍弃旧版的消息卡片
  - 新增对话目录，可快速跳转至指定对话
  - 新增“回到顶部”按钮，可快速回到对话顶部（也可用于回到底部）
  - 新增 HTML 代码块预览
  - 新增消息内容划选栏，可对消息内容进行划选、复制、引用、提问
  - 支持消息转入后台生成
* 对话历史
  - 新增对话置顶、重命名、标签功能
  - 新增对话搜索功能，同时支持按标签筛选
  - 后台消息支持终止生成
* 独立窗口模式
  - 界面布局重大调整
  - 消息历史直接位于界面左侧，可拖拽调整宽度，可隐藏/展开
  - 响应式布局，支持窄屏、宽屏

